const template = `

# サマリ

クロンバックのアルファは、複数の変数がどれくらい一貫して同じ概念を測っているかを表す指標です。以下の<%= num_variables %>個の変数を用いて信頼性分析を行いました。

<%= variables.map(variable => '- ' + variable.name).join('\\n') %>

{{summary}}

この<%= num_variables %>個の変数の**<%= coefficient_name %>**は<%= alpha_value %>で、一般的には**<%= alpha_label %>**と解釈できます。そのため、これらの変数を平均または合計して1つの尺度スコアとして扱うことは、<% if (alpha_acceptable) { %>概ね妥当と考えられます<% } else { %>再検討する余地があります<% } %>。

<%= (has_flagged ? '一方で、**「' + flagged_item + '」** は他の変数との関連が比較的弱く、' + (flagged_alpha_value ? 'この変数を除外するとアルファが' + flagged_alpha_value + 'まで上がるため、' : 'この変数を除外するとアルファが上がるため、') + '他の変数とはやや異なる側面を測っている可能性があります。' : '') %>

# アルファのガイドライン

<div markdown="1" class="markdown-spec-table">

| アルファ | 解釈 |
| :--- | :--- |
| 0.90以上 | 非常に高い一貫性 |
| 0.80〜0.90 | 高い一貫性 |
| 0.70〜0.80 | 許容できる一貫性 |
| 0.60〜0.70 | やや低い一貫性 |
| 0.60未満 | 一貫性が低い |

</div>

**選択された相関係数のタイプとその理由**

{{correlation_method}}

# 各変数を除外した場合のアルファ

各変数を除外した場合に、全体のアルファがどのように変化するかを示します。現在のアルファである**<%= alpha_value %>よりも高くなる**変数は、尺度の一貫性を下げている可能性があります。

{{alpha_dropped_bar}}

<%= (has_alpha_increasing_items ? alpha_increasing_items.map(item => '- 「' + item.name + '」を除外するとアルファは' + alpha_value + 'から' + item.alpha + 'に上がります。').join('\\n') + '\\n\\nこれは、これらの変数が他の変数と比べてやや異なる内容を測っている可能性を示しています。' : '') %>

{{alpha_dropped_table}}

ある変数を除外したときにアルファが現在値より低下する場合、その変数は尺度全体の一貫性を高めることに貢献しています。

除外によるアルファの低下が大きいほど、その変数は尺度の一貫性に強く貢献していると考えられます。

# 相関関係

## 各変数と全体スコアとの相関

各変数と全体スコアの相関の強さを示します。全体スコアとは、全変数を足し上げた合計スコアのことです。主に見るべき指標は、各変数とその変数を除いた残りの変数による合計スコアとの相関である\`各変数抜きの全体スコアとの相関\`です。

{{item_stats}}

相関係数の値が大きいほど、その変数が尺度全体とよく整合していることを意味します。目安として、0.7以上で強い相関、0.5〜0.7で中程度、0.3〜0.5で弱め、0.3未満は注意が必要です。

## 指標の解説

<div markdown="1" class="markdown-spec-table">

| 指標 | 何を見ているか | 実務での使い方 |
| :--- | :--- | :--- |
| 各変数抜きの全体スコアとの相関 | その変数と、その変数を除いた残り変数の合計スコアとの相関 | 最も直感的で実務向き |
| 全体スコアとの相関 | その変数と、全変数の合計スコアとの相関 | 高めに出やすいので参考値 |
| 全体スコアとの相関（標準化） | 変数を標準化したうえでの、変数と全体スコアとの相関 | 変数ごとの分散が違うときの参考値 |
| 各変数との重複を補正した相関 | 変数自身が全体スコアに含まれていることによる重複を補正した指標 | より補正された指標。 |

</div>

## 変数間の相関

変数間の相関を見ることで、どの変数同士が近い内容を測っているかを確認できます。相関が高い組み合わせが上に来るように並べています。赤は正の相関、青は負の相関を表します。

{{correlation_matrix}}

もし一部の変数が他の変数と弱い相関関係にある場合、その変数を同じ尺度に含めるべきか再検討する必要があります。

# 回答分布

各変数の回答分布です。分布の違いは、変数間の相関やアルファに影響することがあります。

{{response_dist}}

# 推奨案

全ての変数を使う場合と、アルファが上がる変数を除外した場合のアルファを比較します。

{{scale_candidates}}

全ての変数から尺度スコアを作成することも可能ですが、一部の変数に絞った方が内的整合性は高くなる可能性があります。変数を除外するかどうかは、統計指標だけでなく、分析目的や変数設計上の意味も考慮して判断する必要があります。

`;

module.exports = template;
