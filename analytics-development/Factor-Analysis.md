const template =`

# サマリ

以下の<%= number_of_variables %>個の変数を元に因子分析を行い、変数間の相関関係から<%= factor_count %>個の潜在的な共通因子を特定しました。これら<%= factor_count %>個の因子でデータ全体のばらつきの<%= cumulative_variance_pct %>%を説明できます。現在の因子数は<%= factor_count %>ですが、[「設定」](//analytics/settings)より変更可能です。

<% variables.forEach(variable => { %>
  - <%= variable.name %>
<% }); %>

<% if (typeof factor_summaries !== 'undefined' && factor_summaries.length > 0) { %>
各因子と特に関連の強い変数は、以下のとおりです。

<% factor_summaries.forEach(fs => { %><% var factor_label = (typeof fs.factor_number === 'number') ? ('第' + fs.factor_number + '因子') : fs.factor; %><% if (fs.has_variables) { %>
- **<%= factor_label %>** は、主に <%= fs.variables.map(v => '「' + v + '」').join('') %> と関連しています。
<% } else { %>
- **<%= factor_label %>** は、十分に強く関連する変数がありません。
<% } %><% }); %>

<% if (warning_variables && warning_variables.length > 0) { %>一方で、<%= warning_variables.map(v => '「' + v + '」').join('') %> などは、クロスロードまたは解釈が難しい変数として判定されています。<% } else { %>クロスロードや解釈が難しい変数は、大きな問題としては見られません。<% } %>
<% } %>

<% if (typeof low_communality_variables !== 'undefined') { %>
<% if (low_communality_variables.length > 0) { %>また、<%= low_communality_variables.map(v => '「' + v + '」').join('') %> は共通性が低く、今回の因子では十分に説明されていない可能性があります。<% } else { %>共通性が極端に低い変数は見られません。<% } %>
<% } %>

# 適性診断

因子を解釈する前に、選択した変数が因子分析に適しているかを確認します。

{{suitability_table}}

**KMO指標**は、選択した変数群が因子分析に向いているかを示す指標です。値は0から1の範囲で、1に近いほど適していると解釈できます。**Bartlettの球面性検定**は、変数間に因子分析を行うだけの相関があるかを確認する検定で、P値が小さいほど変数間に十分な相関があると考えられます。

いずれかの指標がデータの弱さを示している場合は、以下の結果を利用する前に変数の選び直しを検討してください。

# 因子数の選択

因子数を決めるために、以下のスクリープロットでは、実データの固有値、ランダムデータから期待される固有値（平行分析）、および固有値 = 1 の線（カイザー基準）を表示しています。

{{scree_plot_parallel}}

実データの固有値がランダムデータの固有値を上回っている因子は、偶然（ノイズ）ではなくデータに含まれる構造を表している可能性が高いといえます。固有値が1以上の因子は、カイザー基準では残す候補となります。

{{factor_count_table}}

判断方法によって推奨される因子数は異なることがあるため、これらは目安として扱ってください。以下の因子の解釈しやすさとあわせて、<%= factor_count %>個という因子数が妥当かを判断できます。

# 回転方法

現在の回転方法は **<%= rotation_type %>** です。回転は、各変数ができるだけ少数の因子に強く関連するように因子の軸を調整する処理で、因子に名前をつけやすくなります。

<% if (is_oblique) { %>
これは因子同士の相関を許す**斜交回転**です。因子同士が関連していると想定される場合は、斜交回転が適切な選択です。因子間の相関は「Correlations」タブで確認できます。
<% } else { %>
これは因子同士を独立と仮定する**直交回転**です。因子同士が関連している可能性がある場合（例えば連動しやすい嗜好など）は、ObliminやPromaxなどの斜交回転も試すことをおすすめします。
<% } %>

# 因子と変数の関係

以下の表は、各変数が各因子とどの程度関連しているかを示しています。セル内のバーは因子負荷量の大きさを表し、赤は正の負荷量、青は負の負荷量を表します。一般的に、絶対値が0.4以上の場合は一定の関連があり、0.6以上の場合は強い関連があると解釈できます。

{{factor_loadings_table}}

以下のレーダーチャートは、各変数の因子負荷量の絶対値を因子ごとに可視化したものです。ある因子の線が特定の変数のところで外側に伸びていれば、その因子はその変数と関連が強いといえます。

{{factor_loadings_radar}}

## 注意が必要な変数

一部の変数は、特定の因子だけに明確に属するとは限りません。複数の因子にまたがって負荷している変数（クロスロード）や、どの因子にも強く負荷していない変数は、因子に名前をつけて解釈する際に注意が必要です。単一の因子に明確に負荷している変数を優先し、これらの変数は慎重に扱ってください。

# 変数間の関係

第1因子と第2因子で表した平面上に全ての変数間の関係を可視化したのが以下の「バイプロット」と呼ばれるチャートです。

{{biplot}}

元のデータのばらつきは<%= number_of_variables %>個の変数によって表されるものですが、それを<%= factor_count %>個因子のうちの最初の2つの因子をX軸、Y軸にした２次元（X軸とY軸）空間で、できる限り元のデータの持つ情報量（ばらつき）を失わないように可視化しようとしたものです。

それぞれの変数は中心点（0, 0）から外側に向かって伸びるグレーの線によって表されています。線が同じ方向に向かっている変数どうしは相関が強いと言えます。

# 各変数の説明度

以下のチャートは、各変数のばらつきのうち、抽出された因子によって説明できる割合（共通性）と、説明できない割合（独自性）を示しています。

{{communality_bar}}

{{communality_table}}

<% if (improper_communality_variables && improper_communality_variables.length > 0) { %>
注意: <%= improper_communality_variables.map(v => '\`' + v + '\`').join('、') %>の共通性が1を超えています。共通性は本来0〜1の範囲に収まる指標です。1を超える場合は、その変数が「100%以上説明されている」という意味ではなく、因子分析の推定が不安定になっている可能性があります。因子数を減らす、対象変数を見直す、欠損値処理や推定方法を変更することを検討してください。詳しくは、[こちらのノート](<%= heywood_note_url %>)をご覧ください。
<% } %>

**共通性が低い**変数は、今回の因子構造では十分に捉えられておらず、今の因子では説明できない独自の傾向を表している可能性があります。重要な変数の共通性が低い場合は、因子数を増やす、変数の組み合わせを見直すなどを検討してください。

# 各因子の説明率

以下のチャートは、各因子が元の変数全体のばらつきをどの程度説明しているかを示しています。

{{variance_explained}}

棒は各因子の寄与率、線は累積寄与率を表します。<%= factor_count %>個の因子を合わせると、元の変数全体のばらつきの<%= cumulative_variance_pct %>%を説明しています。累積寄与率は因子数を増やすほど高くなるため、因子数は寄与率だけで決めるのではなく、スクリープロット、平行分析、解釈しやすさとあわせて判断してください。

# 因子得点

以下の表は、元のデータに「因子得点」を追加したものです。

{{data}}

因子得点は平均0、標準偏差1に標準化されています。値が0に近いほど平均的であり、0から大きく離れるほど（正負どちらの方向でも）その因子に対して強い傾向を持つ観測値といえます。目安として、±1を超える値は平均から大きく離れた強い傾向を示していると考えられます。

因子得点の正負の解釈は、因子負荷量の符号とあわせて行います。

* 因子負荷量がプラスの変数は、因子得点が高いほどその変数の値も高くなります。
* 因子負荷量がマイナスの変数は、因子得点が高いほどその変数の値は低くなります。

因子得点は、セグメント比較、クラスタリング、回帰分析、予測などの後続の分析に利用できます。

<% if (has_category) { %>

# 因子とカテゴリの関係

カテゴリとして選択された'<%= category %>'と因子の関係をレーダーチャートを使って可視化したものが以下です。

{{category_factor_relationship}}

このチャートは、各カテゴリごとにそれぞれの因子における因子得点の平均値を可視化しています。これにより、特定のカテゴリがどの因子と強く関連しているかを把握できます。

<% } %>

# 補足情報

## 有意性

{{summary}}

ここでの有意性に関する情報は、分析の目的が「仮定した因子構造がデータと適合するかを検証」する場合に有用です。目的がデータから因子構造を「発見」することであれば、ここでの情報は現実的にはあまり意味がありません。

<% if (p > baseline_p) { %>
仮説検定としてカイ2乗検定が行われています。帰無仮説は「因子モデル（構造）とデータは適合する」です。P値は<%= p_value %>で、有意水準<%= baseline_p_pct %>%よりも大きいため、帰無仮説を棄却できず、この因子モデルはデータと適合していると判断できます。
<% } else { %>
仮説検定としてカイ2乗検定が行われています。帰無仮説は「因子モデル（構造）とデータは適合する」です。P値は<%= p_value %>で、有意水準<%= baseline_p_pct %>%よりも小さいため、帰無仮説を棄却でき、この因子モデルはデータと適合していないと判断できます。
<% } %>

<% if (rmsr || rmsea || tli) { %>
上の「サマリー」の表には適合度の指標も表示されています。目安として、RMSRとRMSEAは小さいほど、TLIは1に近いほど、モデルの当てはまりが良いことを示します。
<% } %>
`;

module.exports = template;
