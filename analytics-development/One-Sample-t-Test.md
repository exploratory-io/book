const template = `

# サマリ

<% if (!repeat_by) { %>

<%= target %>の平均が、仮説平均 <%= hypothesized_mean %> と異なるかどうかを調べるために、1標本t検定を行いました。

{{summary}}

<% if (p > baseline_p) { %>
P値は **<%= p_pct %>% (<%= p %>)** で、有意水準 <%= baseline_p_pct %>% (<%= baseline_p %>) より大きいため、<%= target %>の平均は、仮説平均 <%= hypothesized_mean %> と統計的に有意な差があるとは言えません。

ただし、「統計的に有意でない」という結果は、平均が仮説平均と同じであることを証明するものではありません。効果量、信頼区間、サンプルサイズ、検出力もあわせて確認することが重要です。

  <% if (power < 0.5) { %>
検出力は <%= power_pct %>% (<%= power %>) と低くなっています。これは、実際には仮説平均との差が存在していたとしても、この検定ではそれを見逃す可能性が高いことを意味します。結果の解釈には注意が必要です。
  <% } else if (power < 0.8) { %>
検出力は <%= power_pct %>% (<%= power %>) とやや低くなっています。これは、実際には仮説平均との差が存在していたとしても、この検定ではそれを見逃す可能性があることを意味します。結果の解釈には注意が必要です。
  <% } %>

<% } else { %>
P値は **<%= p_pct %>% (<%= p %>)** で、有意水準 <%= baseline_p_pct %>% (<%= baseline_p %>) 以下であるため、<%= target %>の平均は、仮説平均 <%= hypothesized_mean %> と統計的に有意に異なると言えます。

  <% if (effect_size < 0.2) { %>
ただし、Cohen's dで測定した効果量は <%= effect_size %> で、小さいと考えられます。これは、仮説平均との差が実用上は小さい可能性があることを意味します。
  <% } else if (effect_size < 0.8) { %>
Cohen's dで測定した効果量は <%= effect_size %> で、中程度と考えられます。
  <% } else { %>
Cohen's dで測定した効果量は <%= effect_size %> で、大きいと考えられます。
  <% } %>
<% } %>

<% } else { %>

<%= repeat_by %>の各グループごとに、<%= target %>の平均が、仮説平均 <%= hypothesized_mean %> と異なるかどうかを調べるために、1標本t検定を行いました。

{{summary}}

<% if (groups.some(group => group.p <= baseline_p)) { %>
以下のグループでは、P値が有意水準 <%= baseline_p_pct %>% (<%= baseline_p %>) 以下であるため、平均は仮説平均と統計的に有意に異なると言えます。
  <% groups.forEach(group => { %>
    <% if (group.p <= baseline_p) { %>
* <%= group.name %>
    <% } %>
  <% }); %>
<% } %>

<% if (groups.some(group => group.p > baseline_p)) { %>
以下のグループでは、P値が有意水準 <%= baseline_p_pct %>% (<%= baseline_p %>) より大きいため、平均は仮説平均と統計的に有意な差があるとは言えません。
  <% groups.forEach(group => { %>
    <% if (group.p > baseline_p) { %>
* <%= group.name %>
    <% } %>
  <% }); %>
<% } %>

複数のグループに対して検定を行っているため、偶然に統計的に有意な結果が得られることがあります。結果を解釈する際には、P値だけでなく、効果量、信頼区間、サンプルサイズ、実用上の意味もあわせて確認してください。

<% } %>

この検定の有意水準は <%= baseline_p_pct %>% (<%= baseline_p %>) に設定されています。有意水準は[設定](//analytics/settings)から変更できます。


{start_show_hide}
## 統計指標の説明

* t値
  * t値は、サンプル平均が仮説平均からどれだけ離れているかを、標準誤差で割って標準化した統計量です。
  * サンプル平均と仮説平均の差を標準誤差で割ることで計算されます。
  * 絶対値が大きいほど、サンプル平均が仮説平均と異なる可能性が高いことを示します。一般的には、絶対値が2前後以上である場合、自由度や有意水準にもよりますが、差がある可能性が高いと考えられます。

* P値
  * P値は、帰無仮説が正しいと仮定したときに、観測されたサンプル平均と同じか、それ以上に極端な結果が得られる確率を表します。
  * この検定における帰無仮説は、「<%= target %>の平均は、仮説平均 <%= hypothesized_mean %> と等しい」というものです。
  * 一般的に、P値が5%（0.05）未満の場合、統計的に有意であると判断されます。
  * P値は0から1の値を取り、小さいほど帰無仮説に反する証拠が強いことを示します。

* 自由度
  * 1標本t検定における自由度は、データの中で独立して変動できる値の数を表します。
  * サンプルサイズから1を引いた値として計算されます。
  * 自由度が大きくなるほど、t分布は正規分布に近づきます。

* 平均
  * 平均は、サンプルデータにおける<%= target %>の平均値です。
  * この検定では、このサンプル平均が指定された仮説平均と統計的に異なるかどうかを評価します。

* 仮説平均
  * 仮説平均は、この検定で比較対象として指定された基準値です。
  * 1標本t検定では、サンプル平均がこの値と統計的に異なるかどうかを調べます。

* 差
  * 差は、サンプル平均から仮説平均を引いた値です。
  * 正の値の場合、サンプル平均が仮説平均より高いことを示します。
  * 負の値の場合、サンプル平均が仮説平均より低いことを示します。
  * 差の大きさは、信頼区間やP値とあわせて解釈することが重要です。

* 信頼区間の下限
  * 信頼区間の下限は、真の平均が含まれると推定される範囲の下限を示します。
  * 95%信頼区間の場合、真の平均がこの値より低い確率は、設定された信頼水準の片側分に相当する2.5%未満と考えられます。

* 信頼区間の上限
  * 信頼区間の上限は、真の平均が含まれると推定される範囲の上限を示します。
  * 95%信頼区間の場合、真の平均がこの値より高い確率は、設定された信頼水準の片側分に相当する2.5%未満と考えられます。
  * 信頼区間が広いほど推定の精度は低く、狭いほど推定の精度は高いと言えます。
  * 信頼区間に仮説平均が含まれていない場合、サンプル平均は仮説平均と統計的に有意に異なると判断できます。

* Cohen's d
  * Cohen's dは効果量の指標で、サンプル平均と仮説平均の差を、サンプルの標準偏差で割ることで計算されます。
  * 0.2は「小さい効果」、0.5は「中程度の効果」、0.8以上は「大きい効果」と解釈されることがよくあります。
  * 効果の大きさを評価する際には、通常、Cohen's dの絶対値を使います。

* 検出力
  * 検出力は、真の平均が仮説平均と実際に異なる場合に、その差を正しく検出できる確率です。
  * 値は0から1の間を取り、一般的には0.8（80%）以上が望ましいとされます。
  * 検出力が低い場合、実際に存在する差を見逃している可能性があります。

* 第2種の誤り
  * 第2種の誤りは、実際には仮説平均との差が存在するにもかかわらず、「差がない」と判断してしまう確率です。
  * 値は0から1の間を取り、検出力 = 1 - 第2種の誤り、という関係があります。
  * 一般的には、0.2（20%）以下が望ましいとされます。

* 行数
  * 行数は、分析に使用されたデータの数、つまりサンプルサイズを示します。
  * サンプルサイズが大きいほど、統計検定の検出力が高まり、結果の信頼性も向上します。
  * 必要なサンプルサイズは、効果量、データのばらつき、有意水準によって異なります。
{end_show_hide}


# 統計的有意性

統計的有意性は、P値または信頼区間を使って評価できます。両側検定の場合、5%の有意水準に基づくP値での判断は、95%信頼区間に仮説平均が含まれるかどうかという判断と対応します。

## 統計的有意性 - P値

この検定の帰無仮説は、「<%= target %>の平均は、仮説平均 <%= hypothesized_mean %> と等しい」です。

<% if (!repeat_by) { %>

<% if (p > baseline_p) { %>
P値は <%= p_pct %>% (<%= p %>) です。これは、帰無仮説が正しい、つまり<%= target %>の平均が仮説平均 <%= hypothesized_mean %> と等しいと仮定したときに、今回観測されたサンプル平均と同じか、それ以上に極端な結果が得られる確率を表します。

P値が有意水準 <%= baseline_p_pct %>% より大きいため、帰無仮説を棄却することはできません。したがって、<%= target %>の平均は、仮説平均と統計的に有意な差があるとは言えません。
<% } %>

<% if (p <= baseline_p) { %>
P値は <%= p_pct %>% (<%= p %>) です。これは、帰無仮説が正しい、つまり<%= target %>の平均が仮説平均 <%= hypothesized_mean %> と等しいと仮定したときに、今回観測されたサンプル平均と同じか、それ以上に極端な結果が得られる確率を表します。

P値が有意水準 <%= baseline_p_pct %>% 以下であるため、帰無仮説を棄却できます。したがって、<%= target %>の平均は、仮説平均と統計的に有意に異なると言えます。
<% } %>

以下のチャートは、観測されたt値がt分布上のどこに位置しているかを示しています。薄い青色の領域は、有意水準に基づく棄却域を表します。観測されたt値が棄却域に入っている場合、サンプル平均は仮説平均と統計的に有意に異なると判断されます。

<% } else { %>

P値は、帰無仮説が正しい、つまり各グループにおける<%= target %>の平均が仮説平均 <%= hypothesized_mean %> と等しいと仮定したときに、観測されたサンプル平均と同じか、それ以上に極端な結果が得られる確率を表します。

P値が有意水準 <%= baseline_p_pct %>% より大きい場合、帰無仮説を棄却できないため、結果は統計的に有意ではありません。P値が <%= baseline_p_pct %>% 以下の場合、帰無仮説を棄却できるため、結果は統計的に有意です。

以下のチャートは、<%= repeat_by %>の各グループについて、観測されたt値がt分布上のどこに位置しているかを示しています。薄い青色の領域は、有意水準に基づく棄却域を表します。観測されたt値が棄却域に入っている場合、サンプル平均は仮説平均と統計的に有意に異なると判断されます。

<% } %>

{{probability_dist}}

現在の有意水準は <%= baseline_p_pct %>% (<%= baseline_p %>) に設定されています。有意水準は[設定](//analytics/settings)から変更できます。

以下のチャートは、同じ検定を<%= target %>の元の値のスケールで示したものです。真の平均が仮説平均 <%= hypothesized_mean %> と等しいと仮定したときの、サンプル平均の分布を表しています。点線は観測されたサンプル平均を示し、塗りつぶされた領域は有意水準に基づく棄却域を示します。観測されたサンプル平均が塗りつぶされた領域に入っている場合、<%= target %>の平均は仮説平均と統計的に有意に異なると判断されます。

{{probability_dist_value}}


## 統計的有意性 - 信頼区間

<% if (!repeat_by) { %>

以下のチャートは、<%= target %>の平均とその95%信頼区間をエラーバーで可視化したものです。

この95%信頼区間は、<%= target %>の真の平均が含まれると考えられる推定範囲です。信頼区間に仮説平均 <%= hypothesized_mean %> が含まれている場合、サンプル平均は仮説平均と統計的に有意な差があるとは言えません。信頼区間に仮説平均 <%= hypothesized_mean %> が含まれていない場合、サンプル平均は仮説平均と統計的に有意に異なると言えます。

<% } else { %>

以下のチャートは、<%= repeat_by %>の各グループについて、<%= target %>の平均とその95%信頼区間をエラーバーで可視化したものです。

この95%信頼区間は、各グループにおける<%= target %>の真の平均が含まれると考えられる推定範囲です。信頼区間に仮説平均 <%= hypothesized_mean %> が含まれている場合、サンプル平均は仮説平均と統計的に有意な差があるとは言えません。信頼区間に仮説平均 <%= hypothesized_mean %> が含まれていない場合、サンプル平均は仮説平均と統計的に有意に異なると言えます。

<% } %>

{{confidence_interval}}

<% if (!repeat_by) { %>

<% if (p > baseline_p) { %>
この信頼区間には仮説平均 <%= hypothesized_mean %> が含まれているため、<%= target %>の平均は、仮説平均と統計的に有意な差があるとは言えません。
<% } %>

<% if (p <= baseline_p) { %>
この信頼区間には仮説平均 <%= hypothesized_mean %> が含まれていないため、<%= target %>の平均は、仮説平均と統計的に有意に異なると言えます。
<% } %>

<% } %>


# 効果量

統計的有意性を示すP値は、サンプルサイズの影響を受けます。サンプルサイズが大きい場合、仮説平均との差が小さくても統計的に有意になることがあります。一方で、サンプルサイズが小さい場合、実用上意味のある差があっても統計的に有意にならないことがあります。

そのため、P値だけでなく効果量も確認することが重要です。この検定では、効果量としてCohen's dを計算しています。Cohen's dは、<%= target %>のサンプル平均が仮説平均からどれだけ離れているかを、データのばらつきに対して標準化した指標です。

Cohen's dを解釈するための一般的な目安は以下の通りです。

| 効果量 | 解釈 |
|--------|------|
| 0.2 | 小さい効果 |
| 0.5 | 中程度の効果 |
| 0.8 | 大きい効果 |

<% if (!repeat_by) { %>

<% if (effect_size < 0.2) { %>
Cohen's dで測定した効果量は <%= effect_size %> で、小さいと考えられます。
<% } else if (effect_size < 0.8) { %>
Cohen's dで測定した効果量は <%= effect_size %> で、中程度と考えられます。
<% } else { %>
Cohen's dで測定した効果量は <%= effect_size %> で、大きいと考えられます。
<% } %>

<% if (p <= baseline_p && effect_size < 0.2) { %>
結果は統計的に有意ですが、効果量は小さくなっています。仮説平均との差が実用上意味のある大きさかどうかを確認することが重要です。
<% } else if (p > baseline_p && effect_size >= 0.8) { %>
結果は統計的に有意ではありませんが、効果量は大きくなっています。これは、サンプルサイズが十分でないために差を検出できなかった可能性を示しています。信頼区間と検出力もあわせて確認してください。
<% } %>

<% } else { %>

P値が統計的に有意かどうかにかかわらず、効果量を確認することが重要です。結果が統計的に有意であっても、効果量が小さい場合は、仮説平均との差が実用上は小さい可能性があります。一方で、結果が統計的に有意でなくても、効果量が大きい場合は、サンプルサイズが十分でないために差を検出できなかった可能性があります。

<% } %>


# 検出力

検出力は、ある大きさの差が実際に存在する場合に、その差を検出できる確率です。第2種の誤りは、実際には差が存在するにもかかわらず、その差を検出できない確率です。

一般的に、検出力は80%（0.8）以上が望ましいとされています。検出力がそれより低い場合、実際に存在する差を見逃す可能性があるため、結果の解釈には注意が必要です。

<% if (!repeat_by) { %>

<% if (p > baseline_p) { %>

<% if (power < 0.5) { %>
検出力は <%= power_pct %>% (<%= power %>) と低くなっています。実際には仮説平均との差が存在していたとしても、この検定ではそれを見逃す可能性が高いことを意味します。そのため、「統計的に有意でない」という結果は慎重に解釈する必要があります。第2種の誤りは **<%= type2_error_pct %>% (<%= type2_error %>)** です。
<% } else if (power < 0.8) { %>
検出力は <%= power_pct %>% (<%= power %>) とやや低くなっています。実際には仮説平均との差が存在していたとしても、この検定ではそれを見逃す可能性があります。そのため、「統計的に有意でない」という結果は慎重に解釈する必要があります。第2種の誤りは **<%= type2_error_pct %>% (<%= type2_error %>)** です。
<% } else { %>
検出力は <%= power_pct %>% (<%= power %>) と十分に高くなっています。これは、ある程度の大きさの差が存在する場合、この検定でその差を検出できる可能性が高いことを示しています。第2種の誤りは <%= type2_error_pct %>% (<%= type2_error %>) です。
<% } %>

<% } else { %>
結果が統計的に有意であるため、仮説平均との差の大きさや実用上の意味を評価するには、効果量と信頼区間を確認することが特に重要です。
<% } %>

<% } else { %>

検出力と第2種の誤りは、特に統計的に有意でないグループの結果を解釈する際に重要です。結果が統計的に有意でない場合でも、検出力が低い場合は、サンプルサイズが十分でないために差を検出できなかった可能性があります。

<% } %>


# 記述統計

## 統計量

以下の表は、<%= target %>の記述統計量を示しています。

{{statistics}}

## データの分布

以下のチャートは、<%= target %>の分布を可視化したものです。

分布の形、ばらつき、外れ値の有無を確認することで、平均値だけでは見えないデータの特徴を理解しやすくなります。また、平均値がデータ全体を代表する値として適切かどうかを判断する助けになります。

{{distribution}}

## 平均の信頼区間

<% if (!repeat_by) { %>

以下のチャートは、<%= target %>の平均とその信頼区間をエラーバーで可視化したものです。基準線は、仮説平均 <%= hypothesized_mean %> を表しています。

{{mean}}

<% } else { %>

以下のチャートは、<%= repeat_by %>の各グループについて、<%= target %>の平均とその信頼区間をエラーバーで可視化したものです。基準線は、仮説平均 <%= hypothesized_mean %> を表しています。

{{mean}}

<% } %>


# 前提条件と注意点

1標本t検定の結果を解釈する際には、以下の点に注意してください。

* ターゲット変数は数値型である必要があります。
* データは、関心のある母集団から無作為に抽出されたサンプルであることが望まれます。
* 各観測値は互いに独立している必要があります。
* この検定は、<%= target %>の平均が、指定された仮説平均 <%= hypothesized_mean %> と異なるかどうかを評価します。
* 分布が大きく歪んでいる場合や、多くの外れ値を含む場合は、結果の解釈に注意が必要です。
* サンプルサイズが小さい場合、平均の推定が安定しないことがあります。
* 結果が統計的に有意であっても、仮説平均との差が実用上意味のある大きさとは限りません。効果量と信頼区間もあわせて確認してください。
* 結果が統計的に有意でない場合でも、平均が仮説平均と同じであることを証明するものではありません。検出力と信頼区間もあわせて確認してください。

<% if (repeat_by) { %>
* <%= repeat_by %>の各グループに対して複数回の検定を行っているため、偶然に統計的に有意な結果が得られることがあります。結果を解釈する際には、P値だけでなく、効果量、信頼区間、サンプルサイズ、実用上の意味もあわせて確認してください。
<% } %>


`;

module.exports = template;
