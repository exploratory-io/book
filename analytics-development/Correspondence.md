const template = `
<% var isCA = (typeof analysis_type !== 'undefined' && analysis_type === 'CA'); %>
<% var methodName = isCA ? '対応分析' : '多重対応分析'; %>
<% var isOneDim = (typeof n_dims !== 'undefined' && n_dims !== null && Number(n_dims) === 1); %>
<% var _alpha = (typeof alpha !== 'undefined' && alpha !== null && alpha !== '') ? alpha : 0.05; %>

# サマリ

以下のカテゴリ変数<% if (isCA) { %>の関係<% } else { %>に含まれるカテゴリ間の関係<% } %>を分析するために、<%= methodName %>を実施しました。

<% if (typeof variables !== 'undefined' && variables && variables.length > 0) { variables.forEach(function(v){ %>
- <%= (typeof v === 'object' && v.name) ? v.name : v %>
<% }); } %>

<% if (isCA) { %>対応分析は、2つのカテゴリ変数のクロス集計に含まれるカテゴリ間の関係を、少数の次元に要約して可視化する手法です。各カテゴリの分布パターンを比較することで、どのカテゴリが特徴的か、また、どのカテゴリの組み合わせが関連している可能性があるかを探索できます。<% } else { %>多重対応分析は、複数のカテゴリ変数に含まれるカテゴリ間の関係を、少数の次元に要約して分析する手法です。カテゴリ同士の関連や、データに含まれる主要なカテゴリパターンを把握できます。<% } %>

<% if (typeof main_variables !== 'undefined' && main_variables) { %>分析の結果、次元1と次元2への寄与が大きい変数は、<%= String(main_variables).split(',').map(function(v){ return '「' + v.trim() + '」'; }).join('と') %>です。<% } %><% if (typeof cumulative_1_2 !== 'undefined' && cumulative_1_2 !== null) { %>次元1と次元2で、カテゴリ間の関係の<%= cumulative_1_2 %>%を表しています。<% } %>

{{analysis_summary_table}}

# 変数間の関連

<% if (typeof associated_pair_labels !== 'undefined' && associated_pair_labels && associated_pair_labels.length > 0) { %><%= associated_pair_labels.join('、') %>の間に統計的な関連が確認されました。<% if (typeof unclear_pair_count !== 'undefined' && unclear_pair_count > 0) { %>その他の変数の組み合わせでは、統計的に明確な関連は確認されませんでした。<% } %><% } else if (typeof total_pair_count !== 'undefined' && total_pair_count > 0) { %>変数の組み合わせ間に、統計的に明確な関連は確認されませんでした。<% } %>

{{association_table}}

* 複数の変数の組み合わせを同時に検定すると、偶然に有意となる結果が増えやすいため、調整済みP値はHolm法により補正されています。調整方法は設定より変更できます。
* 統計的な関連の判定には、調整済みP値を使用しています。有意水準は α = <%= _alpha %> です。設定より変更できます。
* CramérのVは、変数間の関連の強さを0から1の範囲で表します。関連の強さは、クロス集計表のカテゴリ数を考慮した基準により、「ほとんどない」「弱い」「中程度」「強い」に分類されます。

{start_show_hide}

## 判定のガイドライン

**統計的な関連：**

| 条件 | 判定 |
|---|---|
| 調整済みP値が欠損 | 判定不可 |
| 調整済みP値 < <%= _alpha %> | あり |
| 調整済みP値 ≥ <%= _alpha %> | 明確ではない |

**関連の強さ：**

| 条件 | 関連の強さ |
|---|---|
| CramérのVが欠損 | 判定不可 |
| \\\\(V < 0.10/\\sqrt{k}\\\\) | ほとんどない |
| \\\\(0.10/\\sqrt{k} \\le V < 0.30/\\sqrt{k}\\\\) | 弱い |
| \\\\(0.30/\\sqrt{k} \\le V < 0.50/\\sqrt{k}\\\\) | 中程度 |
| \\\\(V \\ge 0.50/\\sqrt{k}\\\\) | 強い |

\\\\(k\\\\) は以下の式で求めます。

\\\\[ k = \\min(\\text{行のカテゴリ数} - 1,\\ \\text{列のカテゴリ数} - 1) \\\\]

{end_show_hide}

<% if (typeof sparse_warning !== 'undefined' && sparse_warning) { %>
一部のカテゴリの組み合わせでは期待件数が小さいため、カイ二乗近似による検定結果が不安定な可能性があります。低頻度カテゴリの統合を検討してください。
<% } %>

# カテゴリ間の関係

## 2次元マップ

選択された変数に含まれるカテゴリを同じ2次元空間上に配置し、カテゴリ間の関係や特徴的なパターンを表しています。

{{category_plot}}

<% if (isOneDim) { %>片方の変数が2カテゴリであるため、この対応分析で得られる次元は第1次元のみです。第2次元は存在しないため、上のチャートでは各カテゴリのY座標を便宜上0として表示しています。カテゴリ間の関係は、X軸上の位置と距離から確認してください。

これは情報が失われたことを意味するものではありません。2カテゴリの変数が持つ関連パターンは1本の軸ですべて表すことができ、その情報が第1次元に集約されています。

<% } %>**チャートの見方：**

* 原点から離れているカテゴリほど、データ全体の一般的なパターンとは異なる特徴を持っています。
* 異なる変数のカテゴリが同じ方向に位置する場合、似た傾向を持つ可能性があります。反対方向に位置する場合は、対照的な傾向を持つ可能性があります。
* 同じ変数に属するカテゴリ同士の距離は、直接的な関連の強さを表すものではありません。
* チャート上の近さだけでは、統計的に有意な関連があるとは判断できません。
<% if (typeof cumulative_1_2 !== 'undefined' && cumulative_1_2 !== null) { %>* このマップは、次元1と次元2によって、カテゴリ間の関係の<%= cumulative_1_2 %>%を表しています。
<% } %>* 各カテゴリの位置を解釈する際は、下にある表の次元1と次元2の表現品質（cos²）を合計した「2次元マップの表現品質」も確認します。値が高いカテゴリほど、このマップ上で特徴がよく表現されていると解釈できます。

## 各次元の特徴

以下の表は、各次元を特徴づける主なカテゴリを要約したものです。各次元の正側と負側を特徴づけるカテゴリを比較することで、その次元が表す対照的な傾向を確認できます。

正側と負側の向き自体に固定された意味はなく、両側に位置するカテゴリの違いから次元を解釈します。主なカテゴリには、その次元への寄与が大きく（寄与度が高い）、かつその次元上で比較的よく表現されている（表現品質が高い）カテゴリを表示しています。

{{dimension_summary_table}}

## 各次元の説明率

以下のチャートは、各次元がカテゴリ間の関係に含まれる違いをどの程度表しているかを示します。バーは各次元の説明率を、線はその次元までの累積説明率を表します。

{{contribution_chart}}

<% if (typeof cumulative_1_2 !== 'undefined' && cumulative_1_2 !== null) { %>次元1と次元2の累積説明率は<%= cumulative_1_2 %>%です。2次元マップは、この<%= cumulative_1_2 %>%の情報を使ってカテゴリ間の関係を表しています。残りの<%= (100 - cumulative_1_2).toFixed(1) %>%は次元3以降に含まれているため、2次元マップはカテゴリ間の関係の一部を表したものとして解釈してください。<% } %>

<% if (!isCA) { %>多重対応分析では、変数やカテゴリの数が多いほど、カテゴリ間の関係が多くの次元に分散する傾向があります。そのため、次元1と次元2だけの累積説明率が低くなることは珍しくありません。累積説明率が低い場合は、「次元とカテゴリの関係」で次元3以降の結果もあわせて確認してください。<% } %>

## 次元とカテゴリの関係

以下の表は、各カテゴリの座標、次元への寄与度、表現品質（cos²）を、次元ごとに示しています。

座標の正側と負側に位置するカテゴリを比較することで、その次元が表す対照的な傾向を確認できます。寄与度が高いカテゴリほど、その次元を形づくるうえで重要です。表現品質（cos²）が高いほど、そのカテゴリの特徴がその次元でよく表現されています。各次元名の括弧内には、その次元の説明率を表示しています。

{{dimension_matrix_table}}

次元1と次元2のcos²を合計することで、各カテゴリの特徴が2次元マップ上でどの程度表されているかを確認できます。値は0から1の範囲を取り、1に近いほど、2次元マップの中で該当のカテゴリの特徴がよく表現されていると解釈できます。例えば、次元1のcos²が0.21、次元2のcos²が0.22の場合、2次元マップの表現品質は0.43です。このカテゴリの特徴の43%が、次元1と次元2によって表されていると解釈できます。

## 指標の説明

### 座標

カテゴリが各次元のどこに位置するかを示します。正負の方向自体に固定された意味はないため、同じ次元上にある他のカテゴリとの相対的な位置関係を確認できます。

### 寄与度

そのカテゴリが、各次元を形づくるうえでどの程度寄与しているかを示します。各次元の寄与度の合計は100%です。平均寄与度（100% ÷ カテゴリ総数）を上回るカテゴリは、その次元を特徴づけるうえで比較的重要だと言えます。

### cos²（表現品質）

そのカテゴリの特徴が、各次元によってどの程度表されているかを示します。0から1の範囲を取り、値が大きいほど、そのカテゴリの特徴がその次元でよく表現されています。

| cos²          | 解釈の目安            |
| :------------ | :--------------- |
| 0.70以上        | よく表されている    |
| 0.40以上、0.70未満 | 比較的よく表されている |
| 0.20以上、0.40未満 | 一部が表されている   |
| 0.20未満        | この次元だけでの解釈は限定的   |

## カテゴリの組み合わせごとの偏り

各変数ペアのカテゴリの組み合わせについて、実測件数と、変数間に関連がないと仮定した場合の期待件数を比較します。

表は、調整済み標準化残差の絶対値が大きい順に並んでいます。上位ほど、実測件数が期待件数より多い、または少ないという特徴が明確な組み合わせです。

実測件数、期待件数、実測／期待比をあわせて確認することで、偏りの方向と大きさを把握できます。

{{featured_table}}

複数のカテゴリの組み合わせを同時に検定すると、偶然に有意となる結果が増えやすいため、判定にはHolm法で補正した\`調整済みP値\`を使用しています。有意水準は<%= _alpha %>です。判定は統計的な有意性を示すものであり、偏りの実務的な大きさを表すものではありません。

変数全体の関連を評価するカイ二乗検定と、個別のカテゴリの組み合わせを評価する検定では、確認している対象が異なります。そのため、変数全体では明確な関連が確認されなくても、一部の組み合わせで偏りが示されることがあります。その場合は、局所的な傾向として探索的に解釈してください。
<% if (typeof sparse_expected_count_warning !== 'undefined' && sparse_expected_count_warning) { %>
一部の組み合わせでは期待件数が小さいため、P値の近似精度が低い可能性があります。該当する結果は、実測件数や調整済み標準化残差も確認し、探索的な傾向として解釈してください。
<% } %>

## 調整済み標準化残差

各カテゴリの組み合わせについて、実測件数が、2つの変数に関連がないと仮定した場合の期待件数から、どの程度ずれているかを調整済み標準化残差で示しています。正の値は期待より多く、負の値は期待より少ないことを表します。絶対値が大きいほど、期待件数からの偏りが大きいことを示します。

{{residual_heatmap}}

**色の見方：**

条件 | 色 | 意味
-- | -- | --
残差 ≥ 3 | 濃い青 | 期待より大幅に多い
2 ≤ 残差 < 3 | 中間の青 | 期待より多い
0 < 残差 < 2 | 薄い青 | 期待よりやや多い
残差 = 0 | グレー | 期待件数と同じ
-2 < 残差 < 0 | 薄い赤 | 期待よりやや少ない
-3 < 残差 ≤ -2 | 中間の赤 | 期待より少ない
残差 ≤ -3 | 濃い赤 | 期待より大幅に少ない

±2と±3は、期待件数からの偏りの大きさを把握するための目安です。

## カテゴリの組み合わせの行数と割合

各変数の組み合わせについて、カテゴリごとの行数と割合を表形式で示します。

{{proportion_table}}

## カテゴリの組み合わせの割合（チャート）

一方のカテゴリの中で、もう一方のカテゴリがどのような割合（%）で分布しているかを、100%積み上げの横棒グラフで示します（横軸は割合 %）。チャート上部の「変数の組み合わせ」ドロップダウンで、表示する変数の組み合わせを切り替えられます。バーの内訳の違いを比べることで、カテゴリによって分布のパターンがどれくらい異なるかを確認できます。

{{proportion_chart}}

# 詳細データ

## 次元とカテゴリの詳細指標

以下の表では、分析対象となったすべてのカテゴリと次元について、座標、寄与度、表現品質、件数などの詳細な結果を確認できます。

{{category_details_table}}

## 次元スコアを追加したデータ

以下の表は、<%= methodName %>によって算出された各次元のスコアを、元のデータに追加したものです。次元スコアを使用することで、観測値を他の属性で比較したり、チャートで可視化したり、別の分析手法の入力として利用したりできます。

{{data}}
`;

module.exports = template;
