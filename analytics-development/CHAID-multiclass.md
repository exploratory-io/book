const template = `

# 1. サマリ

選択された説明変数 <%= predictor_count %> 個から、<%= target %>を予測するCHAID決定木モデルを作成しました。CHAIDは目的変数の構成に統計的な差がある説明変数を選び、似たカテゴリを統合しながら、特徴の異なるグループへ再帰的に分割します。

## データとモデルの基本情報

| 項目 | 値 |
| :-- | :-- |
| 分析方法 | CHAID決定木 |
| 目的変数 | <%= target %>（<%= target_type %>型） |
| カテゴリ数 | <%= target_category_count %> |
| 説明変数の数 | <%= predictor_count %> |
| 分岐に使用された変数の数 | <%= split_variable_count %> |
| 使用行数 | <%= row_count === null ? '取得不可' : row_count %> |
| 分岐ノード数 | <%= split_node_count %> |
| 終端ノード数 | <%= terminal_node_count %> |
| 観測された最大深度 | <%= observed_max_depth %> |
| 評価方法 | <%= test_mode ? 'テストデータ／トレーニングデータ' : 'トレーニングデータ' %> |

## 目的変数の統計値

{{target_distribution}}


## CHAIDの分析設定

| 項目 | 値 |
| :-- | :-- |
| 分岐の有意水準 | <%= alpha_split %> |
| カテゴリ統合の有意水準 | <%= alpha_merge %> |
| カイ二乗統計量 | <%= chi_square %> |
| Bonferroni調整 | <%= bonferroni %> |
| 統合したカテゴリの再分割 | <%= allow_resplit %> |
| 最大深度 | <%= max_depth %> |
| 分岐前の最小行数 | <%= min_split %> |
| 終端ノードの最小行数 | <%= min_bucket %> |
| 最小ノード割合 | <%= min_node_proportion %> |
| 数値変数の区間化 | <%= numeric_binning %> |
| 初期区間数 | <%= numeric_bins %> |
| 欠損値 | <%= missing %> |
| 目的変数のカテゴリ上限 | <%= target_n %> |
| 説明変数のカテゴリ上限 | <%= predictor_n %> |
| 説明変数の最大カテゴリ数 | <%= max_categories %> |

<% if (!has_splits) { %>
設定された有意水準を満たす説明変数がなかったため、決定木の分岐は作成されませんでした。現在のデータと設定では、目的変数との統計的に明確な関係を持つ説明変数が確認されなかったことを示します。
<% } %>

# 2. 特徴的なグループ

カテゴリごとに、最も特徴的な終端ノードのグループ（そのカテゴリの割合が最も高いグループ）を表示します。全体との比較には、そのカテゴリの全体割合に対する比率を表示します。

{{feature_groups_top}}

行数が少ないグループのカテゴリ構成は、新しいデータで大きく変動する可能性があります。比率は因果関係ではなく、この決定木の分割結果として解釈してください。

# 3. 決定木

CHAIDが目的変数の構成の異なるグループを段階的に作成した結果です。1つのノードから3つ以上の枝に分かれることがあり、数値変数は分岐に使われた最終区間で表示します。

{{tree_structure}}

各ノードには目的変数の構成、行数、予測クラスを表示します。枝ラベルには親ノードからの条件を表示します。

# 4. 分岐の詳細

## 4-1. 分岐についての情報

以下の表では、決定木で作られた分岐に使用された説明変数と目的変数との関係を評価した検定結果を確認できます。

{{split_evidence}}

## 4-2. 分岐グループ別カテゴリ構成

各分岐で作られた子ノードの目的変数構成を並べ、なぜその分岐によって異なるグループが作られたのかを確認します。チャートは変数単位ではなく分岐ノード単位で作成します。各チャートでは親ノードの構成比を先に表示し、その分岐で作られた子ノードを続けます。

{start_lazy_show_hide}
{{branch_category_composition}}
{end_lazy_show_hide}

各バーは、この分岐で作られたグループ内の目的変数の構成比を示します。グループ間で構成比の違いが大きいほど、この分岐によって目的変数の異なるグループが作られていることを表します。

## 4-3. カテゴリの統合結果

<% if (has_category_merges) { %>
CHAIDでは、目的変数の構成に統計的な違いが確認されなかったカテゴリをまとめたうえで分岐します。

{{category_merges}}
<% } else { %>
カテゴリの統合はありませんでした。
<% } %>

<% if (has_numeric_predictors) { %>
## 4-4. 数値変数の区間化結果

数値型の説明変数は、最初に複数の区間へ変換され、その後、目的変数の構成が似ている区間が統合されます。

{{numeric_intervals}}
<% } %>

# 5. 予測に対する変数重要度

変数を1つずつシャッフルし、classification log loss の増加量を測定する permutation importance を表示します。値が正の場合、その変数をシャッフルすると予測が悪化したことを示します。評価データは Test Mode では Test、それ以外では Training です。

{start_lazy_show_hide}
{{variable_importance}}
{end_lazy_show_hide}

この重要度は予測性能への寄与を表します。ツリー内で分岐に使われる位置（「分岐の詳細」）とは評価している側面が異なるため、順位が一致しない場合があります。

変数重要度は、目的変数との関係の方向や因果関係を表すものではありません。

# 6. モデルの予測精度

CART版の決定木と同じ評価指標を使用します。テストモードではトレーニングデータとテストデータを分けて表示します。

{{summary}}

{{class_summary}}

## 予測マトリックス

{{confusion_matrix}}
<% if (has_ordered_error) { %>
## カテゴリ誤差の分布

目的変数が順序付きカテゴリのため、予測が実測のカテゴリからどれだけ離れているかを順序尺度で評価できます。0 は正解、±1、±2 … は予測が何カテゴリ分ずれたかを表します。0（および ±1）に集中しているほど誤差は小さく隣接的で、±2 以上に分布が広がるほど大きな順序の誤分類が起きています。

{{category_error_distribution}}
<% } %>

# 7. 予測結果

{start_lazy_show_hide}
### テーブル
{{data}}
{end_lazy_show_hide}

# 8. 改善のための提案

<% if (!has_splits) { %>
現在の設定では、有意水準を満たす分岐が確認されませんでした。分岐の有意水準、分岐前の最小行数、数値変数の初期区間数を確認してください。ただし、有意水準を緩めると偶然の差を分岐として取り込む可能性が高くなります。
<% } %>
<% if (tree_too_complex) { %>
多数の小さなノードが作成されています。分岐の有意水準を厳しくする、カテゴリをより積極的に統合する、ノードの最小行数を増やす、最大深度を小さくすることを検討してください。具体的には、「分割の有意水準」を小さくする、「統合の有意水準」を小さくする、「分岐するノードの最小サイズ」を大きくする、「終端ノードの最小サイズ」を大きくする、「子ノードの最小割合」を大きくする、「決定木の最大の深さ」を小さくする、といった調整が考えられます。
<% } %>
<% if (has_aggressive_merges) { %>
多くのカテゴリが同じグループにまとめられています。カテゴリ統合の有意水準を大きくすることで、カテゴリが別々に残りやすくなります。
<% } %>
<% if (fine_numeric_bins) { %>
数値変数が多数の細かい区間に分かれています。初期区間数を減らすか、終端ノードの最小行数を増やすことを検討してください。
<% } %>
<% if (has_many_small_nodes) { %>
行数の少ない終端ノードが複数作成されています。これらのグループのカテゴリ構成は、新しいデータで大きく変動する可能性があります。
<% } %>
CHAIDは、統計的に異なるセグメントを分かりやすく発見することに適しています。より高い予測性能が必要な場合は、CART、ランダムフォレスト、XGBoost、LightGBM、CatBoostなどとの比較を検討してください。
<% if (has_ordered_error) { %>
実測値から2カテゴリ以上離れた予測が多くなっています。「カテゴリ誤差の分布」と順序付き混同行列を確認し、大きな予測のずれが発生しているグループを調べてください。
<% } %>

`;
module.exports = template;
