const template = `

# サマリ

<% if (!repeat_by) { %>

<%= explanatory %>によって、<%= target %>の平均に差があるかどうかを2標本t検定で調べました。

<% if (var_equal) { %>
この検定では、2つのグループの分散が等しいと仮定するt検定を使用しています。
<% } else { %>
この検定では、2つのグループの分散が等しいとは仮定しないWelch（ウェルチ）のt検定を使用しています。
<% } %>

{{summary}}

<% if (p > baseline_p) { %>
P値は **<%= p_pct %>% (<%= p %>)** で、有意水準 <%= baseline_p_pct %>% (<%= baseline_p %>) より大きいため、<%= explanatory %>による<%= target %>の平均の差は統計的に有意とは言えません。

ただし、統計的に有意でないことは、「差がない」と証明されたことを意味するわけではありません。効果量、信頼区間、サンプルサイズ、検出力を合わせて確認することが重要です。

  <% if (power < 0.5) { %>
検出力は <%= power_pct %>% (<%= power %>) と低く、実際に差があったとしても検出できない可能性が高いため、結果の解釈には注意が必要です。
  <% } else if (power < 0.8) { %>
検出力は <%= power_pct %>% (<%= power %>) とやや低めです。実際に差があったとしても検出できない可能性があるため、結果の解釈には注意が必要です。
  <% } %>

<% } else { %>
P値は **<%= p_pct %>% (<%= p %>)** で、有意水準 <%= baseline_p_pct %>% (<%= baseline_p %>) 以下であるため、<%= explanatory %>による<%= target %>の平均の差は統計的に有意だと言えます。

  <% if (effect_size < 0.2) { %>
ただし、効果量（Cohen's d）は <%= effect_size %> と小さいため、平均の差は実務的には小さい可能性があります。
  <% } else if (effect_size < 0.8) { %>
効果量（Cohen's d）は <%= effect_size %> であり、平均の差は中程度と考えられます。
  <% } else { %>
効果量（Cohen's d）は <%= effect_size %> と大きく、平均の差も大きいと考えられます。
  <% } %>
<% } %>

<% } else { %>

<%= repeat_by %>ごとに、<%= explanatory %>によって<%= target %>の平均に差があるかどうかを2標本t検定で調べました。

<% if (var_equal) { %>
この検定では、それぞれの<%= repeat_by %>の中で、2つのグループの分散が等しいと仮定するt検定を使用しています。
<% } else { %>
この検定では、それぞれの<%= repeat_by %>の中で、2つのグループの分散が等しいとは仮定しないWelch（ウェルチ）のt検定を使用しています。
<% } %>

{{summary}}

<% if (groups.some(group => group.p <= baseline_p)) { %>
以下のグループでは、P値が有意水準 <%= baseline_p_pct %>% (<%= baseline_p %>) 以下であるため、平均の差は統計的に有意だと言えます。
  <% groups.forEach(group => { %>
    <% if (group.p <= baseline_p) { %>
* <%= group.name %>
    <% } %>
  <% }); %>
<% } %>
<% if (groups.some(group => group.p > baseline_p)) { %>
以下のグループでは、P値が有意水準 <%= baseline_p_pct %>% (<%= baseline_p %>) より大きいため、平均の差は統計的に有意とは言えません。
  <% groups.forEach(group => { %>
    <% if (group.p > baseline_p) { %>
* <%= group.name %>
    <% } %>
  <% }); %>
<% } %>

複数のグループで検定を行っているため、偶然に有意と判定される結果が含まれる可能性があります。P値だけでなく、効果量、信頼区間、サンプルサイズ、実務的な意味も合わせて確認してください。

<% } %>

この検定における有意水準は <%= baseline_p_pct %>% (<%= baseline_p %>) に設定されています。有意水準、および2つのグループの分散が等しいと仮定するかどうかは、アナリティクスの[「設定」](//analytics/settings)から変更できます。


{start_show_hide}
## 統計指標の説明

* 差
  * 差は比較する2つのグループの平均値の差を表します。
  * 差の値そのものは測定単位によって異なり、正の値は第一グループの平均が高いことを、負の値は第二グループの平均が高いことを示します。
  * 差の大きさだけでなく、信頼区間とP値を合わせて解釈することが重要です。

* t値
  * t値は2つの平均値の差が偶然によるものか、本当に異なるのかを示す統計量です。手元のデータの平均の差を標準誤差で割った値です。
  * 値の絶対値が大きいほど、平均値の差が偶然ではない可能性が高いことを示します。一般的には絶対値が2以上で意味がある差と考えられます。

* P値
  * P値は観測されたデータが帰無仮説（グループ間に差がない）と同等かそれ以上に極端である確率を示します。
  * 一般的に5%（0.05）未満であれば、統計的に有意であると判断されます。
  * 値は0から1の間で、P値が小さいほど統計的有意性が高いことを示します。

* 自由度
  * 自由度はt検定において、データが取りうる自由な変動の数を表します。一般に「全サンプル数 - グループ数」で計算されます。
  * 常に正の整数値を取り、値が大きいほど検定の精度が高まります。
  * 自由度が大きくなるにつれてt分布は正規分布に近づきます。

* 信頼区間下限
  * 信頼区間下限は、統計的に推定された平均値の差の下限を示します。
  * この値より低い差である確率は、設定した信頼水準（通常95%）の半分（2.5%）未満です。

* 信頼区間上限
  * 信頼区間上限は、統計的に推定された平均値の差の上限を示します。
  * この値より高い差である確率は、設定した信頼水準（通常95%）の半分（2.5%）未満です。
  * 信頼区間の幅が広いほど推定の精度が低く、狭いほど精度が高いことを示します。
  * 信頼区間に0が含まれない場合（下限がプラスか、上限がマイナス）、統計的に有意な差があると判断できます。

* ベースレベル
  * ベースレベルは比較の基準となるグループや条件を示します。
  * 差や効果量はこのベースレベルと比較した相対的な値として表されます。

* Cohen's D
  * Cohen's Dは効果の大きさを示す指標で、グループ間の差をデータのばらつき（標準偏差）で割った値です。
  * 値が0.2の場合は「小さい効果」、0.5は「中程度の効果」、0.8以上は「大きい効果」と解釈されます。

* 検出力
  * 検出力は実際に効果がある場合に、それを統計的に検出できる確率を表します。
  * 値は0から1の間で、一般的には0.8（80%）以上が望ましいとされています。
  * 検出力が低い場合、実際に存在する効果を見逃している可能性があることを示します。

* タイプ2エラー
  * タイプ2エラーは「実際には効果があるのに、ないと判断してしまう確率」です。
  * 値は0から1の間で、検出力 = 1 - タイプ2エラーの関係があります。
  * 一般的には0.2（20%）以下が望ましいとされています。

* 行数
  * 行数は分析に使用したデータの総数（サンプルサイズ）を示します。
  * サンプルサイズが大きいほど、統計的検定の検出力が高まり、結果の信頼性が向上します。
  * 検定の種類や効果量によって、必要なサンプルサイズは異なります。
{end_show_hide}

# 有意性

有意性は、P値または信頼区間を使って判断できます。両側検定の場合、有意水準5%でのP値による判断と、95%信頼区間に0が含まれるかどうかによる判断は一致します。


## 有意性 - P値

この検定における帰無仮説は「<%= group_a %>と<%= group_b %>の<%= target %>の平均に差がない」というものです。

<% if (!repeat_by) { %>

<% if (p > baseline_p) { %>
検定の結果、P値は <%= p_pct %>% (<%= p %>) でした。これは、帰無仮説が正しい、つまり2つのグループの平均に差がないと仮定した場合に、今回観測された差と同じくらい、またはそれ以上に極端な差が得られる確率を表します。

P値が有意水準 <%= baseline_p_pct %>% より大きいため、帰無仮説を棄却できません。そのため、<%= explanatory %>による<%= target %>の平均の差は統計的に有意であるとは言えません。

<% } %>

<% if (p <= baseline_p) { %>
検定の結果、P値は <%= p_pct %>% (<%= p %>)でした。これは、帰無仮説が正しい、つまり2つのグループの平均に差がないと仮定した場合に、今回観測された差と同じくらい、またはそれ以上に極端な差が得られる確率を表します。

P値が有意水準<%= baseline_p_pct %>%以下であるため、帰無仮説を棄却できます。そのため、<%= explanatory %>による<%= target %>の平均の差は統計的に有意であると言えます。

<% } %>

確率分布であるt分布のどのあたりに観測されたt値が位置するのかを可視化したのが以下のチャートです。薄い青の領域は、有意水準にもとづく棄却域です。観測されたt値が棄却域に入る場合、平均の差は統計的に有意であると判断されます。

<% } else { %>

P値は、帰無仮説が正しい、つまり2つのグループの平均に差がないと仮定した場合に、今回観測された差と同じくらい、またはそれ以上に極端な差が得られる確率を表します。

P値が有意水準 <%= baseline_p_pct %>% より大きければ、帰無仮説を棄却できないため、統計的に有意であるとは言えません。逆に、P値が有意水準 <%= baseline_p_pct %>% 以下であれば、帰無仮説を棄却できるため、統計的に有意であると言えます。

確率分布であるt分布のどのあたりに観測されたt値が位置するのかを、<%= repeat_by %>ごとに可視化したのが以下のチャートです。薄い青の領域は、有意水準にもとづく棄却域です。観測されたt値が棄却域に入る場合、平均の差は統計的に有意であると判断されます。

<% } %>

{{probability_dist}}

現在の有意水準は<%= baseline_p_pct %>% (<%= baseline_p %>)に設定されています。有意水準はアナリティクスの[「設定」](//analytics/settings)から変更できます。

以下のチャートは、同じ検定を平均値の差のスケールで表したものです。差がないと仮定したときの平均差の分布を示しており、中心は0です。点線は今回観測された平均差を示し、薄い青の領域は有意水準にもとづく棄却域です。観測された平均差が薄い青の領域に入る場合、平均の差は統計的に有意であると判断されます。

{{probability_dist_diff}}

## 有意性 - 信頼区間

<% if (!repeat_by) { %>

<%= group_a %>と<%= group_b %>の<%= target %>の平均の差と、その95%信頼区間を、エラーバーを使って可視化したのが以下のチャートです。

この95%信頼区間は、母集団における平均の差がどの範囲にありそうかを示す推定範囲です。信頼区間に0（差がない）が含まれていれば、<%= target %>の平均の差は統計的に有意であるとは言えません。逆に、0が含まれていなければ、平均の差は統計的に有意であると言えます。

<% } else { %>

<%= group_a %>と<%= group_b %>の<%= target %>の平均の差と、その95%信頼区間を、<%= repeat_by %>ごとにエラーバーを使って可視化したのが以下のチャートです。

この95%信頼区間は、母集団における平均の差がどの範囲にありそうかを示す推定範囲です。信頼区間に0（差がない）が含まれていれば、<%= target %>の平均の差は統計的に有意であるとは言えません。逆に、0が含まれていなければ、平均の差は統計的に有意であると言えます。

<% } %>


{{confidence_interval}}

<% if (!repeat_by) { %>

<% if (p > baseline_p) { %>
この区間に0（差がない）が含まれているため、<%= explanatory %>による<%= target %>の平均の差は統計的に有意であるとは言えません。
<% } %>

<% if (p <= baseline_p) { %>
この区間に0（差がない）が含まれていないため、<%= explanatory %>による<%= target %>の平均の差は統計的に有意であると言えます。
<% } %>

<% } %>

# 効果量

統計的有意性を示すP値は、サンプルサイズの影響を受けます。サンプルサイズが大きい場合、実務的には小さな差でも統計的に有意と判定されることがあります。一方で、サンプルサイズが小さい場合、実際には意味のある差があっても有意と判定されないことがあります。

そのため、P値だけでなく、効果量を使って差の大きさを確認することが重要です。この検定では、効果量としてCohen's dを計算しています。Cohen's dは、<%= explanatory %>による<%= target %>の平均値の違いを、データのばらつきで標準化した指標です。

効果量（Cohen's d）の大きさの目安は以下のとおりです。


| 効果量の値 | 効果量の大きさ |
|------------|------------|
| 0.2 | 小さい効果 |
| 0.5 | 中程度の効果 |
| 0.8 | 大きい効果 |

<% if (!repeat_by) { %>

<% if (effect_size < 0.2) { %>
今回の効果量（Cohen's d）は <%= effect_size %> であり、小さい効果と考えられます。
<% } else if (effect_size < 0.8) { %>
今回の効果量（Cohen's d）は <%= effect_size %> であり、中程度の効果と考えられます。
<% } else { %>
今回の効果量（Cohen's d）は <%= effect_size %> であり、大きい効果と考えられます。
<% } %>

<% if (p <= baseline_p && effect_size < 0.2) { %>
P値は統計的に有意ですが、効果量は小さいため、実務的に意味のある差かどうかを確認することが重要です。
<% } else if (p > baseline_p && effect_size >= 0.8) { %>
P値は統計的に有意ではありませんが、効果量は大きいため、サンプルサイズ不足などによって差を検出できなかった可能性があります。信頼区間や検出力も合わせて確認してください。
<% } %>

<% } else { %>

効果量は、P値が有意かどうかに関わらず確認することが重要です。有意であっても効果量が小さい場合は、実務的には小さな差である可能性があります。一方で、有意でない場合でも効果量が大きい場合は、サンプルサイズ不足などによって差を検出できなかった可能性があります。

<% } %>

# 検出力

検出力は、実際に一定の大きさの差が存在すると仮定した場合に、その差を統計的に検出できる確率です。タイプ2エラーは、そのような差が存在するにもかかわらず、検定で検出できない確率です。

一般的に、検出力は80%（0.8）以上あることが望ましいとされています。それよりも低い場合は、実際に差があっても検出できない可能性があるため、結果の解釈に注意が必要です。


<% if (!repeat_by) { %>

<% if (p > baseline_p) { %>

<% if (power < 0.5) { %>
検出力は **<%= power_pct %>% (<%= power %>)** と低い値になっています。実際に差があったとしても、この検定では検出できない可能性が高いため、「有意ではない」という結果の解釈には慎重になる必要があります。タイプ2エラーは **<%= type2_error_pct %>% (<%= type2_error %>)** です。
<% } else if (power < 0.8) { %>
検出力は **<%= power_pct %>% (<%= power %>)** とやや低めです。実際に差があったとしても、この検定では検出できない可能性があるため、「有意ではない」という結果の解釈には注意が必要です。タイプ2エラーは **<%= type2_error_pct %>% (<%= type2_error %>)** です。
<% } else { %>
検出力は **<%= power_pct %>% (<%= power %>)** と十分に高い水準です。今回の検定では、一定の大きさの差があれば検出できる可能性が高いと考えられます。タイプ2エラーは **<%= type2_error_pct %>% (<%= type2_error %>)** です。
<% } %>

<% } else { %>
今回は統計的に有意と判断されているため、主に効果量と信頼区間を確認し、差の大きさや実務的な意味を評価することが重要です。
<% } %>

<% } else { %>

検出力やタイプ2エラーは、特に統計的に有意ではないグループの結果を解釈する際に重要です。有意ではない場合でも、検出力が低ければ、サンプルサイズ不足などによって差を検出できなかった可能性があります。


<% } %>

# 記述統計情報

## 統計値

それぞれのグループごとの統計値は以下の通りです。

{{statistics}}

## データの分布

<%= explanatory %>による<%= target %>の分布の違いを、密度曲線を使って可視化したものが以下です。

分布の形、ばらつき、外れ値の有無を確認することで、平均値の差だけでは見えないグループ間の違いを理解しやすくなります。

{{distribution}}

## 平均値の信頼区間

それぞれのグループごとの平均値と信頼区間を、エラーバーを使って可視化したものが以下です。

{{mean}}

# 前提条件と注意点

2標本t検定では、以下の点に注意してください。

* 比較する2つのグループは互いに独立している必要があります。
* 目的変数は数値型の変数である必要があります。
* 各グループの分布が大きく歪んでいる場合や、外れ値が多い場合は、結果の解釈に注意が必要です。
* サンプルサイズが小さい場合は、平均値の差が安定して推定されないことがあります。
* 統計的に有意であっても、差が実務的に意味のある大きさとは限りません。効果量や信頼区間も合わせて確認してください。
* 統計的に有意でない場合でも、「差がない」と証明されたわけではありません。検出力や信頼区間を確認することが重要です。

<% if (var_equal) { %>
* この検定では、2つのグループの分散が等しいと仮定しています。グループ間で分散やサンプルサイズが大きく異なる場合は、Welch（ウェルチ）のt検定の使用を検討してください。
<% } else { %>
* この検定では、2つのグループの分散が等しいとは仮定しないWelch（ウェルチ）のt検定を使用しています。分散やサンプルサイズがグループ間で異なる場合にも使いやすい方法です。
<% } %>

<% if (repeat_by) { %>
* <%= repeat_by %>ごとに複数回の検定を行っているため、偶然に有意と判定される結果が出やすくなる可能性があります。P値だけでなく、効果量、信頼区間、サンプルサイズ、実務的な意味も合わせて確認してください。
<% } %>

# 補足情報

## 次のステップ

* 今回は<%= target %>に対する<%= explanatory %>という1つの変数による平均の差を分析しました。しかし、他の変数も<%= target %>に影響している可能性があります。そのため、「線形回帰」などの多変量分析を使うことで、複数の要因を同時に考慮した分析を行うことができます。
<% if (!repeat_by) { %>
* 今回の検定は全体に対して行われましたが、グループごとに分けて実施することも可能です。その場合は、「繰り返し」にグループとなる変数を選択して、再度実行できます。
<% } %>
* 複数の指標をまとめて一括で検定したい場合は、データの形を変更することで対応できます。詳しくは、[こちら](https://exploratory.io/note/exploratory/mxW2zKb2)のノートをご覧ください。

`;

module.exports = template;
