const template = `


<% if (algorithm == 'pearson') { %>
全ての変数の組み合わせに対し相関係数を計算しました。相関係数の計算のアルゴリズムは「ピアソン」が使われています。アルゴリズムは[「設定」](//analytics/settings/do_cor_argo)より変更可能です。
<% } else if (algorithm == 'spearman') { %>
全ての変数の組み合わせに対し相関係数を計算しました。相関係数の計算のアルゴリズムは「スピアマン」が使われています。アルゴリズムは[「設定」](//analytics/settings/do_cor_argo)より変更可能です。
<% } else { %>
全ての変数の組み合わせに対し相関係数を計算しました。相関係数の計算のアルゴリズムは「ポリコリック」が使われています。アルゴリズムは[「設定」](//analytics/settings/do_cor_argo)より変更可能です。

<% } %>

# 相関の強さ

<% if (!repeat_by) { %>
全ての変数の組み合わせの相関係数を計算し、それぞれの値を以下のルールに従って色付けし、ヒートマップと呼ばれる表にまとめました。

<% } else { %>
全ての変数の組み合わせの相関係数を計算し、それぞれの値を以下のルールに従って色付けし、<%= repeat_by %>ごとにヒートマップと呼ばれる表にまとめました。
<% } %>

* 1（正の相関の最大値）に近いほど濃い赤
* -1（負の相関の最大値）に近いほど濃い青
* 0（相関がない）に近いほどグレー

{{correlation_chart:0.8}}

「相関係数を表示」にチェックを付けるとヒートマップ上に相関係数が表示されます。


## 相関係数の解釈

相関係数は-1から1の間の値をとり、変数間の相関関係の強さと方向を示します。

* 1に近いほど強い正の相関（一方が増加すると他方も増加）
* -1に近いほど強い負の相関（一方が増加すると他方は減少）
* 0に近いほど相関が弱い、または存在しない

相関係数の解釈の目安は以下となります。

* 0.8 ~ 1.0 (または -0.8 ~ -1.0): 非常に強い相関
* 0.6 ~ 0.8 (または -0.6 ~ -0.8): 強い相関
* 0.4 ~ 0.6 (または -0.4 ~ -0.6): 中程度の相関
* 0.2 ~ 0.4 (または -0.2 ~ -0.4): 弱い相関
* 0.0 ~ 0.2 (または -0.0 ~ -0.2): ほとんど相関なし、または無相関

## 注意点

* 相関係数は変数間の因果関係を示すものではなく、あくまで2つの数値型の変数が共に同じ方向に変動するという意味での関係の強さを示す指標です。
* 相関係数のアルゴリズムはデフォルトで**ピアソンの相関係数**が使われていますが、もし変数間に直線的（線形）ではないが何らかの単調な関係がある場合、**スピアマンの順位相関係数**の方が相関関係をより適切に捉えることができます。アルゴリズムは[「設定」](//analytics/settings/do_cor_argo)より変更可能です。
* スピアマンの順位相関係数は元の値を順位としての値に変化したあとに、ピアソンの相関係数と同じ方法で相関係数を計算します。2つの相関係数の詳細は、[こちらのノート](https://exploratory.io/note/exploratory/2-BsF1LQF4)をご参照ください。
* ポリコリック相関係数は、アンケートの評価尺度（例：1から5）のような順序尺度の変数に対して用いる指標です。順序尺度の回答の背後にあると仮定される連続変数間の相関を推定します。


# 相関の有意性

全ての変数の組み合わせにおける相関係数について、統計的に有意かどうかを判断するための仮説検定を行いました。

{{data}}

## 指標の解釈

<% if (algorithm == 'pearson') { %>

* 相関係数（ピアソンの相関係数）
  * 相関係数は、2つの変数間の線形関係の強さと方向を示す統計量です。
  * 値は-1から1の間で、1は完全な正の線形関係、-1は完全な負の線形関係、0は線形関係がないことを示します。

* P値
  * P値は「相関がない（相関係数が0）」という仮説のもとで、観測された相関がどの程度起こりやすいかを示します。
  * 一般的に5%（0.05）未満であれば、統計的に有意であると判断されます。
  * 値は0から1の間で、P値が小さいほど相関がないという仮説に反する証拠が強いことを示します。

* 統計量 (t値)
  * t値は、相関係数が0と有意に異なるかどうかを検定するために用いられる統計量です。
  * 相関係数とその標準誤差から計算されます。
  * 絶対値が大きいほど、相関がない（相関係数が0）という仮説に反する証拠が強くなり、P値は小さくなります。

<% } else if (algorithm == 'spearman') { %>

* 相関係数（スピアマンの順位相関係数）
  * 相関係数は2つの変数間の**単調な関係（増加・減少の傾向）**の強さと方向を示す統計量です。
  * データの値そのものではなく**順位（順序）**に基づいて計算されるため、外れ値の影響を受けにくい特徴があります。
  * 値は-1から1の間で、1は完全な正の単調関係、-1は完全な負の単調関係、0は単調な関係がないことを示します。

* P値
  * P値は観測された相関係数が偶然によるものである確率を示します。
  * 一般的に5%（0.05）未満であれば、統計的に有意であると判断されます。
  * 値は0から1の間で、P値が小さいほど統計的有意性が高いことを示します。

* 統計量
  * 統計量はスピアマンの相関係数が0（無相関）であるかどうかを検定するために使われる値です。
  * データの条件に応じて、以下のいずれかが用いられます。
  * S値（順位差の二乗和）
      * 各データの順位の差を二乗して合計した値で、スピアマン相関の定義に基づく統計量です。
      * サンプルのペア数が少ない場合（行数 < 50）かつタイ（同順位）がない場合に使われます。
      * 値が小さいほど順位のずれが小さく、相関がない（相関係数が0）という仮説に反する証拠が強くなり、P値は小さくなります。
  * Z値（正規近似）
      * 相関係数を標準化して、標準正規分布に従うように変換した統計量です。
      * サンプルのペア数が多い場合（行数 >= 50）またはタイ（同順位）がある場合に使われます。
      * 絶対値が大きいほど、相関がない（相関係数が0）という仮説に反する証拠が強くなり、P値は小さくなります。

<% } else { %>

* 相関係数（ポリコリック相関係数）
  * ポリコリック相関係数は、2つの順序尺度の変数のそれぞれが、正規分布に従う連続的な潜在変数を段階的に区切ったものであると仮定し、その潜在変数間の相関を推定する統計量です。
  * アンケートの評価尺度（例：1から5）のような順序尺度に適しています。
  * 値は-1から1の間で、1は完全な正の相関、-1は完全な負の相関、0は相関がないことを示します。

* P値
  * P値は「相関がない（相関係数が0）」という仮説のもとで、観測された相関がどの程度起こりやすいかを示します。
  * 一般的に5%（0.05）未満であれば、統計的に有意であると判断されます。
  * 値は0から1の間で、P値が小さいほど相関がないという仮説に反する証拠が強いことを示します。

* 統計量 (z値)
  * z値は、相関係数が0と有意に異なるかどうかを検定するために用いられる統計量です。
  * ポリコリック相関係数をその標準誤差で割って計算されます。
  * 絶対値が大きいほど、相関がない（相関係数が0）という仮説に反する証拠が強くなり、P値は小さくなります。

<% } %>

## 検定の仕組み

相関係数の仮説検定では、相関が偶然によるものかどうかを評価します。

* ピアソン相関の場合は、t検定が用いられます。
* スピアマン相関の場合は、データの条件に応じて以下の方法が使われます。
  * 行数が少なく（目安として n < 50）かつタイ（同順位）がない場合：正確検定
  * それ以外の場合：近似検定（正規近似）
* ポリコリック相関の場合は、推定された相関係数の標準誤差に基づくz検定が用いられます。

仮説検定では、それぞれの相関係数に対してP値が計算されます。P値は「相関がない（相関係数が0である）」という仮説のもとで、観測された結果が偶然に得られる確率を示します。

* P値が<%= baseline_p_pct %>%より小さい場合、算出された相関係数は統計的に有意であると判断されます。
* P値が<%= baseline_p_pct %>%以上の場合、算出された相関係数は統計的に有意であるとは言えません。

現在の有意水準（P値）は<%= baseline_p_pct %>% (<%= baseline_p %>)に設定されていますが、これは[「設定」](//analytics/settings)より変更可能です。

## 検定結果の可視化

<% if (!repeat_by) { %>
仮説検定の結果を以下のルールに従って散布図にまとめました。

<% } else { %>
仮説検定の結果を以下のルールに従って<%= repeat_by %>ごとに散布図にまとめました。

<% } %>

* 赤：統計的に有意な正の相関
* 青：統計的に有意な負の相関
* グレー：統計的に有意ではない
* 円の大きさ：相関係数の大きさ

{{significance_chart:0.8}}



# 補足情報

## 次のステップ

<% if (!repeat_by) { %>
* グループ別分析：グループごとに相関分析を実施することで、それぞれのグループ内での相関関係をより詳細に理解できるかもしれません。その場合は、「繰り返し」にグループとなる変数を選択し、実行し直すことができます。
<% } %>
* 外れ値の確認：変数の中で外れ値がないか確認し、必要に応じて対処することで、より正しい関係性の解釈ができる可能性があります。
* 多変量分析：2つの変数間に相関関係があったとしても、それは他の相関関係のある変数による影響である可能性もあります。そこで「線形回帰」などを使った多変量分析を使い、目的となる変数に対して他の変数による効果を考慮したうえで、変数間の関係をより適切に分析することをお薦めします。



`;

module.exports = template;
